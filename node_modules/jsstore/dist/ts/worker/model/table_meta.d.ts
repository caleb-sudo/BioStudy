import { ITable, IAlterQuery, IColumn, TColumns } from "@/common";
export declare class TableMeta {
    name: string;
    columns: IColumn[];
    primaryKey: string;
    autoIncColumnValue: {};
    alter?: IAlterQuery;
    keypath: string | string[];
    constructor(table: ITable);
    setColumn(tableColumns: TColumns): any[];
}
